# Copy this file to `.env` (in this same folder) and paste your key.
# This `.env` is gitignored — your key never gets committed.

GEMINI_API_KEY=

# Optional overrides:
# GEMINI_IMAGE_MODEL=gemini-2.5-flash-image
# IMAGE_OUTPUT_DIR=/absolute/path/to/save/pngs   (defaults to ./static)
